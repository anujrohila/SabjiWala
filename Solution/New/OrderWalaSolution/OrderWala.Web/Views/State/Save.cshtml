@model OrderWala.Domain.tblStateDTO
@{
    ViewBag.Title = "Save";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}


<div id="page-title">
    <h1 class="page-header text-overflow">ADD New State</h1>
</div>

<div class="panel">
    <div class="panel-heading">
        <h3 class="panel-title">State Master</h3>
    </div>
   
    
    <!--Input Size-->
    <!--===================================================-->
  @using(Html.BeginForm("Save","State",FormMethod.Post, new { @class="form-horizontal" }))
    {
      
      @Html.HiddenFor(Model => Model.StateId)
      
        <div class="panel-body">
            @Html.ValidationSummary(true)
            <div class="form-group">
                <div class="text-danger">
                    @Html.ValidationSummary()
                </div>
            </div>
            
            <div class="form-group">
                <label class="col-sm-3 control-label" for="demo-is-inputsmall">State Name</label>
                <div class="col-sm-5">
                    @Html.TextBoxFor(model => model.StateName, new { @class ="form-control input-sm", placeholder ="" })
                    @*<input type="text" placeholder=".input-sm" class="form-control input-sm" id="demo-is-inputsmall">*@
                </div>
            </div>
            
        </div>
        <div class="panel-footer">
            <div class="row">
                <div class="col-sm-9 col-sm-offset-3">
                    <button class="btn btn-success btn-labeled fa fa-save fa-lg" type="submit">Save</button>
                   <a href="@Url.Action("ListAllState","State")" class="btn btn-danger btn-labeled fa fa-repeat fa-lg" >Cancel</a>
                    
                </div>
            </div>
        </div>
    }
    <!--===================================================-->
    <!--End Input Size-->


</div>
